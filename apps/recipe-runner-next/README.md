# Recipe Runner Next

This directory is a clean workspace for the next iteration of the project.

Goals:

- Do not modify the existing `secretary-web` or `terminal-gateway-dotnet` apps while exploring the new architecture.
- Use the saved blueprint as the source of truth for the new implementation direction.
- Keep migration and rollback guidance close to the new project so changes can be staged safely.

Documents:

- `REFACTOR_BLUEPRINT.md`: target architecture and implementation phases.

